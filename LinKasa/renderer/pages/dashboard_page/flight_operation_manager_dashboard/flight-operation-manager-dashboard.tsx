import { Container, Grid, Paper } from "@mui/material";

export default function FlightOperationManagerDashboard() {
  return (
    <Container maxWidth="lg" sx={{ mt: 2, mb: 4 }}>
      ini flight operation manager dashboard
    </Container>
  );
}
