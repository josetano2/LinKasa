import { Container, Grid, Paper } from "@mui/material";

export default function AirportOperationManagerDashboard() {
  return (
    <Container maxWidth="lg" sx={{ mt: 2, mb: 4 }}>
      ini airport operation manager dashboard
    </Container>
  );
}
