import { Container, Grid, Paper } from "@mui/material";

export default function ChiefFinancialOfficerDashboard(){
    return (
        <Container maxWidth="lg" sx={{ mt: 2, mb: 4 }}>
            ini dashboard cfo
        </Container>
    )
}