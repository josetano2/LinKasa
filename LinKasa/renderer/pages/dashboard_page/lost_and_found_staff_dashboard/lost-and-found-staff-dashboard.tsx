import { Container, Grid, Paper } from "@mui/material";

export default function LostAndFoundStaffDashboard(){
    return (
        <Container maxWidth="lg" sx={{ mt: 2, mb: 4 }}>
            ini dashboard lost and found dashboard
        </Container>
    )
}