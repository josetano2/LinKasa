import { Container, Grid, Paper } from "@mui/material";

export default function CustomerServiceManagerDashboard() {
  return (
    <Container maxWidth="lg" sx={{ mt: 2, mb: 4 }}>
      ini customer service manager dashboard
    </Container>
  );
}
