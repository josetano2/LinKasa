import { Container, Grid, Paper } from "@mui/material";

export default function GroundHandlingManagerDashboard(){
    return (
        <Container maxWidth="lg" sx={{ mt: 2, mb: 4 }}>
            ini dashboard ground handling manager
        </Container>
    )
}