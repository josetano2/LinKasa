import { Container, Grid, Paper } from "@mui/material";

export default function AirportDirectorDashboard(){
    return (
        <Container maxWidth="lg" sx={{ mt: 2, mb: 4 }}>
            Ini ceo dashboard
        </Container>
    )
}