import { Container, Grid, Paper } from "@mui/material";

export default function CargoHandlerDashboard(){
    return (
        <Container maxWidth="lg" sx={{ mt: 2, mb: 4 }}>
            ini dashboard cargo handler
        </Container>
    )
}