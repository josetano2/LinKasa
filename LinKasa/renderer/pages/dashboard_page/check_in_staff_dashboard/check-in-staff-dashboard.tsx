import { Container, Grid, Paper } from "@mui/material";

export default function CheckInStaffDashboard() {
  return (
    <Container maxWidth="lg" sx={{ mt: 2, mb: 4 }}>
      ini check in staff dashboard
    </Container>
  );
}
