import { Container, Grid, Paper } from "@mui/material";

export default function MaintenanceManagerDashboard(){
    return (
        <Container maxWidth="lg" sx={{ mt: 2, mb: 4 }}>
            ini dashboard maintenance manager
        </Container>
    )
}