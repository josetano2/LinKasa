import { Container, Grid, Paper } from "@mui/material";

export default function LogisticsManagerDashboard(){
    return (
        <Container maxWidth="lg" sx={{ mt: 2, mb: 4 }}>
            ini dashboard logistics manager
        </Container>
    )
}