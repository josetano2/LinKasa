import { Container, Grid, Paper } from "@mui/material";

export default function CustomsAndBorderControlOfficerDashboard(){
    return (
        <Container maxWidth="lg" sx={{ mt: 2, mb: 4 }}>
            ini dashboard customs and border control officer
        </Container>
    )
}